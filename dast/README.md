# DAST (동적 애플리케이션 보안 테스트)

DAST 폴더입니다. 도구(예: OWASP ZAP 등)와 구조는 자유롭게 정하시면 됩니다.

참고로 `sast/` 워크플로는 `.github/workflows/sast-*.yml`처럼 파일명으로 구분해서 정리해 두었습니다. DAST 워크플로도 `dast-*.yml`처럼 접두어만 맞춰주시면, 같은 레포 안에서 서로 간섭 없이 독립적으로(동시에도) 실행됩니다.
